# lookupData

data.frame of all lookup values Allowing all definitions to be
centralized in Qualification-Template

## Usage

``` r
lookupData
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 33
rows and 18 columns.
